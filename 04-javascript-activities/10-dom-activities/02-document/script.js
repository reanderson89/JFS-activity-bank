// Take 5 or so minutes and explore the "document" object. Read the comments and mess around with the console.logs

// this will show you the entire html document
console.log(document);

// this will show just the body element, and everything inside of it
console.log(document.body);

// this will show the children nodes of the body element
console.log(document.body.children);

// Using your knowledge of accessing objects and arrays. Print to the console the 3rd "p" element from the body
// Use the array that is printed from the above console.log to help you.
console.log()